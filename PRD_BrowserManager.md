# 通用可视化浏览器采集工具 PRD
> 基于 Tauri 2 + React + Python/Playwright 构建的 macOS 开源桌面工具

---

## 一、产品概述

### 1.1 背景
许多站点的数据采集、账号管理和自动化操作都依赖浏览器环境。现有商业指纹浏览器价格高、封闭、定制成本高，不适合开源项目或轻量本地部署场景。

本项目目标是构建一个**面向 macOS 的可视化 Chromium 采集工作台**，用于多 Profile 管理、可见浏览器窗口控制、插件导入、站点任务执行和本地结果存储。

### 1.2 产品定位
- 开源
- macOS 本地桌面应用
- 多 Profile 独立浏览器环境
- 支持可见 Chromium 窗口
- 支持本地 Chrome 插件导入
- 面向通用站点采集，而非单一网站

### 1.3 目标用户
- 需要做网站数据采集的开发者
- 需要运营多个账号并保持会话隔离的人员
- 需要本地调试浏览器自动化流程的研究/工程人员

### 1.4 核心价值
- 一个本地工具管理多个浏览器 Profile
- 每个 Profile 的登录态、插件、代理、缓存独立隔离
- 浏览器可见，便于手工登录和调试
- 采集逻辑可通过 adapter 方式扩展到不同网站

---

## 二、产品目标与范围

### 2.1 目标
- 提供 macOS 可安装、可运行、可维护的本地采集工具
- 支持创建、编辑、启动、停止、删除 Profile
- 支持每个 Profile 独立启动 Chromium 可见窗口
- 支持导入本地 Chrome 插件
- 支持将站点采集逻辑按 adapter 扩展
- 支持本地存储结果并导出

### 2.2 非目标
- 不承诺“一个规则抓遍所有网站”
- 不内置任何站点的绕过方案
- 不做云端账号同步
- 不做多端协同
- 不作为浏览器内核重写项目

---

## 三、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面壳 | Tauri 2.x | macOS 桌面应用壳，负责窗口、菜单、打包、系统能力 |
| 前端 UI | React 18 + TypeScript | 管理界面 |
| 样式 | Tailwind CSS | UI 样式 |
| 后端服务 | Python 3.12 + FastAPI | Profile、任务、浏览器生命周期、结果处理 |
| 浏览器控制 | Playwright | 启动和控制 Chromium 可见窗口 |
| 浏览器内核 | Bundled Chromium | 随应用打包的 Chromium 内核 |
| 数据存储 | SQLite | 本地持久化存储 |
| 前后端通信 | localhost HTTP / Tauri command | 管理壳与后端服务通信 |

### 3.1 技术路线说明
- Tauri/React 只承担管理壳和交互界面
- 真正的采集浏览器不使用 Tauri 内置 WebView
- 每个 Profile 启动一个独立 Chromium 会话
- Chromium 以 persistent context 方式运行，保证目录隔离和插件隔离

---

## 四、核心功能需求

### 4.1 Profile 管理

#### 新建 Profile
- 输入名称、备注
- 选择或填写代理信息
- 导入 Cookie
- 配置基础浏览器参数
- 选择是否启用插件
- 自动生成独立 Profile 目录

#### Profile 字段
- 名称
- 备注
- 代理配置
- Cookie
- 用户数据目录
- 启用插件列表
- 最近启动时间
- 当前状态

#### Profile 列表
- 支持搜索
- 支持按状态筛选
- 支持批量启动、批量停止
- 支持编辑、删除

#### 删除 Profile
- 删除数据库记录
- 删除本地 Profile 目录
- 删除关联缓存和插件工作目录

### 4.2 浏览器会话控制

#### 启动会话
- 点击启动后，创建对应 Profile 的 Chromium 实例
- 浏览器窗口必须可见
- 自动应用代理配置
- 自动加载 Cookie
- 自动加载启用的插件
- 启动后记录会话状态与端口信息

#### 停止会话
- 点击停止后关闭对应 Chromium 实例
- 释放相关进程与端口资源

#### 并发控制
- 支持多个 Profile 并发运行
- 默认并发上限可配置
- 超出上限时进入等待队列

### 4.3 插件管理

#### 支持的插件类型
- 本地 unpacked extension 目录
- 本地 `.crx` 文件

#### 插件导入
- 导入后解包到本地扩展目录
- 校验 `manifest.json`
- 记录插件名称、版本、启用状态

#### 插件启用
- Profile 可选择启用一个或多个插件
- 每次启动浏览器时按 Profile 配置加载插件

#### 插件错误处理
- 缺少 manifest 时提示错误
- manifest 不兼容时提示错误
- 解包失败时提示错误
- 插件错误不得影响其他 Profile 启动

### 4.4 任务中心

#### 任务定义
- 一个 Profile 可绑定一个或多个任务
- 任务支持目标 URL、参数和站点类型
- 任务执行逻辑由 site adapter 提供

#### 任务执行
- 启动指定 Profile 的浏览器会话
- 打开目标站点或页面
- 执行 adapter 逻辑
- 保存结构化结果

#### 任务状态
- 待运行
- 运行中
- 已完成
- 失败

#### 任务失败原因
- 浏览器启动失败
- 代理不可用
- 页面加载超时
- 插件加载失败
- adapter 执行异常

### 4.5 数据结果

#### 结果存储
- 所有任务结果本地存储到 SQLite
- 结果字段采用通用 JSON 结构
- 允许不同站点输出不同字段集合

#### 结果展示
- 支持列表查看
- 支持按任务、Profile、站点筛选
- 支持查看原始 JSON

#### 导出
- 支持导出 CSV
- 支持导出 JSON

### 4.6 设置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 最大并发窗口数 | 5 | 同时运行的浏览器上限 |
| 请求随机延迟 | 2~5 秒 | 页面操作间隔 |
| 浏览器模式 | 可见窗口 | MVP 默认可视化 |
| Profile 存储路径 | `~/Documents/BrowserProfiles` | 本地 Profile 目录 |
| 结果导出路径 | `~/Downloads` | CSV / JSON 导出位置 |
| 插件存储路径 | `~/Documents/BrowserPlugins` | 本地插件目录 |

---

## 五、站点适配框架

### 5.1 设计原则
- 一个站点对应一个 adapter
- adapter 接收 URL、Profile、任务参数
- adapter 输出统一结构化 JSON
- 站点差异通过 adapter 隔离，不污染核心框架

### 5.2 Adapter 接口
- 输入：目标 URL、任务参数、Profile 上下文
- 输出：状态、结果数据、错误信息、耗时

### 5.3 内置适配器
- 示例 adapter
- TikTok adapter 作为首个实际模板
- 后续网站通过独立 adapter 扩展

---

## 六、数据模型

### 6.1 核心表
- `profiles`
- `browser_sessions`
- `tasks`
- `task_runs`
- `result_items`
- `extensions`
- `site_adapters`

### 6.2 数据原则
- 本地优先
- Profile 与结果分离
- 任务与结果可追踪到来源 Profile 和 adapter
- 结果支持半结构化 JSON 存储

---

## 七、UI 页面结构

```
App
├── 侧边栏
│   ├── Profile 管理
│   ├── 浏览器会话
│   ├── 任务中心
│   ├── 数据结果
│   └── 设置
│
├── Profile 管理页
│   ├── 搜索框
│   ├── 新建 Profile
│   ├── 批量启动 / 停止
│   └── Profile 列表
│
├── 浏览器会话页
│   ├── 当前运行会话
│   ├── 会话状态
│   └── 停止 / 重启
│
├── 任务中心页
│   ├── 任务列表
│   ├── 运行状态
│   └── 批量执行
│
├── 数据结果页
│   ├── 结果列表
│   ├── 结果详情
│   └── 导出
│
└── 设置页
    ├── 并发设置
    ├── 存储路径
    ├── 插件目录
    └── 日志与诊断
```

---

## 八、非功能需求

| 项目 | 要求 |
|------|------|
| 平台 | macOS 12+ |
| 交付物 | 可构建 `.app`，GitHub Release 可提供 `.app.zip` |
| 后续交付 | `.dmg`、签名、公证 |
| 浏览器可见性 | 默认可见 |
| 数据安全 | 所有数据本地存储 |
| 启动体验 | 本地快速启动 |
| 可扩展性 | 支持新增站点 adapter |

---

## 九、开发阶段划分

### Phase 1 - MVP
- Tauri/React 管理壳
- Profile CRUD
- Chromium 可见窗口启动/停止
- 本地 Cookie、代理、插件导入
- 示例 adapter
- 本地结果存储与导出
- `.app` 构建

### Phase 2 - 采集扩展
- 更多站点 adapter
- 任务调度增强
- 错误重试与队列
- 结果筛选与检索增强

### Phase 3 - 分发增强
- `.dmg` 安装包
- Developer ID 签名
- Apple notarization
- 发布自动化

---

## 十、已知风险

| 风险 | 缓解措施 |
|------|---------|
| Chromium 版本与插件兼容性差异 | 锁定 Chromium 版本，明确支持范围 |
| 不同站点反爬策略差异大 | 通过 adapter 隔离站点逻辑 |
| 多 Profile 并发带来资源占用 | 设置并发上限和排队机制 |
| 本地插件格式不规范 | 导入时做 manifest 校验与错误提示 |
| macOS 分发受签名限制 | 先用 `.app`，后续补签名与 `.dmg` |

---

## 十一、验收标准

- 能创建、编辑、删除 Profile
- 能启动和停止独立可见 Chromium 会话
- 能为 Profile 加载本地插件
- 能执行至少一个示例 adapter
- 能将结果保存到 SQLite
- 能导出 JSON 和 CSV
- 能构建 macOS `.app`

