# x-browser 界面设计文档

> 基于 PRD 的 macOS 桌面应用 UI 设计规范

---

## 一、设计原则

- **macOS 原生风格**：遵循 Apple Human Interface Guidelines，深色/浅色模式自适应
- **工具导向**：信息密度适中，操作路径短，突出核心功能
- **状态可见**：Profile、会话、任务的状态通过颜色和图标直观表达
- **最小干扰**：侧边栏常驻，内容区最大化利用空间

---

## 二、整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  x-browser                                    ─ □ ×         │  ← Title Bar (macOS 原生)
├──────────┬──────────────────────────────────────────────────┤
│          │                                                  │
│  侧边栏   │              主内容区                            │
│  64px宽   │              (根据导航切换)                        │
│  图标+文字 │                                                  │
│          │                                                  │
│          │                                                  │
├──────────┴──────────────────────────────────────────────────┤
│  状态栏：运行中 Profile 数 | 并发数 | 后端状态                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.1 窗口规格
- 默认尺寸：1280 × 800
- 最小尺寸：900 × 600
- 侧边栏宽度：64px（图标模式）/ 200px（图标+文字，可切换）

### 2.2 侧边栏导航

```
┌────────┐
│  👤    │  Profile 管理
│  🌐    │  浏览器会话
│  ▶️    │  任务中心
│  📊    │  数据结果
│  ⚙️    │  设置
└────────┘
```

- 当前选中项：高亮背景 + 品牌色指示条
- 徽标：未读/异常数量红点提示（任务中心、浏览器会话）

---

## 三、页面详细设计

### 3.1 Profile 管理页

```
┌──────────────────────────────────────────────────────────────┐
│ Profile 管理                              [+ 新建 Profile]   │
├──────────────────────────────────────────────────────────────┤
│ 🔍 搜索 Profile...    [状态 ▼] [批量启动 ▼] [批量停止]        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ● 运行中  抖音主号                                     │   │
│  │    socks5://127.0.0.1:7890  |  2 插件  |  上次: 10分钟前 │   │
│  │    [停止] [重启] [编辑 ▼]                              │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ○ 已停止  测试账号                                     │   │
│  │    无代理  |  0 插件  |  上次: 3天前                    │   │
│  │    [启动] [编辑 ▼] [删除]                              │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 元素说明
- **Profile 卡片**：圆角矩形，浅色背景，悬停阴影
- **状态圆点**：绿色=运行中、灰色=已停止、黄色=启动中、红色=异常
- **操作按钮**：主操作（启动/停止）高亮，次操作为文字按钮
- **搜索**：实时过滤，支持名称、备注、代理地址
- **筛选**：全部 / 运行中 / 已停止 / 有异常

#### 新建/编辑 Profile 弹窗
```
┌──────────────────────────────┐
│ 新建 Profile              ✕  │
├──────────────────────────────┤
│ 名称 *                       │
│ ┌──────────────────────────┐ │
│ │ 抖音主号                  │ │
│ └──────────────────────────┘ │
│                              │
│ 备注                         │
│ ┌──────────────────────────┐ │
│ │ 用于抖音内容采集          │ │
│ └──────────────────────────┘ │
│                              │
│ 代理配置         [类型 ▼]    │
│ ┌──────────────────────────┐ │
│ │ socks5://127.0.0.1:7890  │ │
│ └──────────────────────────┘ │
│                              │
│ Cookie 导入    [选择文件...] │
│ ┌──────────────────────────┐ │
│ │ 已选择: cookies.json      │ │
│ └──────────────────────────┘ │
│                              │
│ 插件                          │
│ ☑️ AdBlock Plus  v3.2.1      │
│ ☐ 翻译助手      v1.0.5       │
│                              │
│     [取消]    [创建并启动]    │
└──────────────────────────────┘
```

---

### 3.2 浏览器会话页

```
┌──────────────────────────────────────────────────────────────┐
│ 浏览器会话                                                  │
├──────────────────────────────────────────────────────────────┤
│ 当前运行 3 / 最大 5                                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 🖥️           │  │ 🖥️           │  │ ⏳           │      │
│  │ 抖音主号      │  │ 快手账号     │  │ 等待中...    │      │
│  │ PID: 12345   │  │ PID: 12346   │  │ 队列位置: 1  │      │
│  │ 端口: 9222   │  │ 端口: 9223   │  │              │      │
│  │ 运行: 15分钟  │  │ 运行: 8分钟   │  │              │      │
│  │              │  │              │  │              │      │
│  │ [停止] [截图] │  │ [停止] [截图] │  │ [取消]       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 元素说明
- **会话卡片**：展示缩略图/图标、Profile 名称、PID、调试端口、运行时长
- **等待队列**：超出并发上限时显示等待中的 Profile
- **截图**：快速截取当前浏览器窗口（用于调试记录）

---

### 3.3 任务中心页

```
┌──────────────────────────────────────────────────────────────┐
│ 任务中心                              [+ 新建任务] [批量执行] │
├──────────────────────────────────────────────────────────────┤
│ 🔍 搜索...  [状态 ▼] [站点 ▼] [Profile ▼]                    │
├──────┬──────────┬──────────┬──────────┬──────────┬───────────┤
│ 名称  │ 站点      │ Profile  │ 状态     │ 耗时      │ 操作      │
├──────┼──────────┼──────────┼──────────┼──────────┼───────────┤
│ 采集1 │ TikTok   │ 抖音主号  │ 🟢 已完成 │ 2m 15s   │ [查看]    │
│ 采集2 │ TikTok   │ 测试账号  │ 🔴 失败   │ 30s      │ [重试]    │
│ 采集3 │ 通用     │ 快手账号  │ 🟡 运行中 │ 1m 30s...│ [停止]    │
└──────┴──────────┴──────────┴──────────┴──────────┴───────────┘
```

#### 元素说明
- **状态标签**：彩色圆点 + 文字（待运行/运行中/已完成/失败）
- **失败原因**：悬停显示详细错误信息
- **批量执行**：选中多个任务后一键顺序/并发执行

#### 新建任务弹窗
```
┌──────────────────────────────┐
│ 新建任务                  ✕  │
├──────────────────────────────┤
│ 任务名称 *                   │
│ ┌──────────────────────────┐ │
│ │ TikTok 主页采集           │ │
│ └──────────────────────────┘ │
│                              │
│ 选择 Profile *    [下拉选择] │
│                              │
│ 目标站点 *        [TikTok ▼] │
│                              │
│ 目标 URL                     │
│ ┌──────────────────────────┐ │
│ │ https://tiktok.com/...   │ │
│ └──────────────────────────┘ │
│                              │
│ 任务参数 (JSON)              │
│ ┌──────────────────────────┐ │
│ │ {"count": 10, ...}       │ │
│ └──────────────────────────┘ │
│                              │
│     [取消]    [创建]         │
└──────────────────────────────┘
```

---

### 3.4 数据结果页

```
┌──────────────────────────────────────────────────────────────┐
│ 数据结果                                       [导出 ▼]      │
├──────────────────────────────────────────────────────────────┤
│ 🔍 搜索...  [站点 ▼] [任务 ▼] [日期范围 ▼]                   │
├──────┬──────────┬──────────┬──────────┬──────────────────────┤
│ ID   │ 站点      │ 任务      │ 采集时间  │ 内容预览              │
├──────┼──────────┼──────────┼──────────┼──────────────────────┤
│ 1001 │ TikTok   │ 采集1     │ 10:23    │ {"title": "...", ...} │
│ 1002 │ TikTok   │ 采集1     │ 10:24    │ {"title": "...", ...} │
└──────┴──────────┴──────────┴──────────┴──────────────────────┘
│                            共 156 条  [上一页] 1 2 3 [下一页] │
└──────────────────────────────────────────────────────────────┘
```

#### 元素说明
- **内容预览**：JSON 首字段折叠显示，点击展开查看完整 JSON
- **导出**：支持 CSV / JSON 两种格式，导出时可选筛选条件
- **详情抽屉**：点击行展开右侧抽屉，展示格式化后的完整数据

---

### 3.5 设置页

```
┌──────────────────────────────────────────────────────────────┐
│ 设置                                                         │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  通用                                                        │
│  ─────────────────────────────────────────────────────────   │
│  最大并发窗口数                    ┌────────┐ [5]            │
│  请求随机延迟 (秒)                 ┌────────┐ [2] ~ ┌────────┐│
│                                                    │ [5]    ││
│  浏览器模式                        [● 可见窗口  ○ 无头]      │
│                                                              │
│  存储                                                        │
│  ─────────────────────────────────────────────────────────   │
│  Profile 存储路径    ┌──────────────────────────┐ [更改...] │
│                      │ ~/Documents/BrowserProf.. │            │
│                      └──────────────────────────┘            │
│  结果导出路径        ┌──────────────────────────┐ [更改...] │
│                      │ ~/Downloads              │            │
│                      └──────────────────────────┘            │
│  插件存储路径        ┌──────────────────────────┐ [更改...] │
│                      │ ~/Documents/BrowserPlug.. │            │
│                      └──────────────────────────┘            │
│                                                              │
│  插件管理                                                    │
│  ─────────────────────────────────────────────────────────   │
│  已导入插件 (3)                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ AdBlock Plus  v3.2.1  [已启用]  [删除]                │   │
│  │ 翻译助手      v1.0.5  [已禁用]  [删除]                │   │
│  └──────────────────────────────────────────────────────┘   │
│  [+ 导入插件 (.crx / 文件夹)]                                │
│                                                              │
│  日志与诊断                                                  │
│  ─────────────────────────────────────────────────────────   │
│  日志级别               [INFO ▼]                             │
│  [查看日志目录]  [导出诊断信息]                               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、配色方案

### 品牌色
| 名称 | 色值 | 用途 |
|------|------|------|
| 品牌主色 | `#2563EB` | 按钮、选中态、链接 |
| 品牌深 | `#1D4ED8` | 悬停态 |
| 成功 | `#10B981` | 运行中、完成 |
| 警告 | `#F59E0B` | 等待中、注意 |
| 错误 | `#EF4444` | 失败、异常、删除 |
| 信息 | `#6B7280` | 次要文字、边框 |

### 暗色模式
- 背景：`#1E1E1E` → `#252526` → `#2D2D30`（层级递进）
- 文字：`#E4E4E4` 主文字 / `#A0A0A0` 次要文字
- 边框：`#3E3E42`

---

## 五、交互规范

| 场景 | 交互方式 |
|------|---------|
| 启动 Profile | 点击启动 → 卡片状态变黄色（启动中）→ 绿色（运行中） |
| 停止 Profile | 点击停止 → 确认弹窗 → 状态变灰色 |
| 删除 Profile | 点击删除 → 二次确认（含"同时删除数据"选项）→ 移除卡片 |
| 任务执行 | 点击执行 → 状态轮询更新 → 完成后通知 |
| 导入插件 | 拖拽或选择文件 → 校验进度 → 成功/失败提示 |
| 导出结果 | 选择格式 → 选择路径 → 进度条 → 完成提示 |

---

## 六、组件清单

| 组件名 | 类型 | 复用位置 |
|--------|------|---------|
| Sidebar | 布局 | 全局 |
| ProfileCard | 卡片 | Profile 管理、浏览器会话 |
| TaskRow | 表格行 | 任务中心 |
| ResultRow | 表格行 | 数据结果 |
| Modal | 弹窗 | 新建/编辑 Profile、新建任务 |
| ConfirmDialog | 确认框 | 删除、停止 |
| StatusBadge | 徽标 | 各列表页 |
| EmptyState | 空状态 | 各列表页 |
| Toast | 通知 | 全局 |
| FileUploader | 文件上传 | 插件导入、Cookie 导入 |
