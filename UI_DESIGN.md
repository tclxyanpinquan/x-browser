# 页面设计稿 v1
> 面向 `Tauri + React` 的 macOS 桌面应用界面设计

## 1. 设计目标

- 这是一个操作型工具，不做营销页风格
- 页面以表格、筛选、抽屉、弹窗为主
- 适合高频扫描、批量操作、状态监控
- 默认浅色主题，支持系统深色模式

## 2. 全局布局

### 2.1 窗口结构

```
┌──────────────────────────────────────────────────────────────┐
│ Top Bar: 面包屑 / 页面标题 / 全局搜索 / 快捷动作 / 账号状态  │
├───────────────┬──────────────────────────────────────────────┤
│ Sidebar       │ Main Content                                 │
│ 1. Profile    │                                              │
│ 2. Sessions   │                                              │
│ 3. Tasks      │                                              │
│ 4. Results    │                                              │
│ 5. Settings   │                                              │
└───────────────┴──────────────────────────────────────────────┘
```

### 2.2 布局原则

- 左侧固定导航，宽度 232px
- 顶部工具栏固定，高度 56px
- 内容区使用全宽带状布局，不做大块悬浮卡片
- 详情用右侧抽屉，不打断列表操作
- 所有主要操作保留图标 + 文本按钮

### 2.3 视觉风格

- 背景：浅灰白
- 主体：白色面板
- 边框：低对比灰线
- 主色：低饱和蓝
- 状态色：绿 / 黄 / 红 / 灰
- 圆角：8px 以下
- 字号：12 / 14 / 16 为主，不做大字号营销感

## 3. 组件规范

### 3.1 常用组件

- 搜索框
- 筛选器
- 分段控制器
- 状态标签
- 表格
- 右侧详情抽屉
- 创建 / 编辑弹窗
- 确认对话框
- Toast 提示
- 空状态

### 3.2 按钮策略

- 主按钮：`+`、`▶`、`■`、`⭳`
- 危险操作：删除、停止、解除绑定
- 文本按钮只用于明确动作
- 工具按钮优先使用图标

## 4. 页面设计

### 4.1 Profile 管理页

#### 页面目的
管理多账号、多代理、多插件的核心入口。

#### 布局

```
Header: Profile 管理 / 搜索 / 筛选 / 新建 / 批量启动 / 批量停止
┌─────────────────────────────────────────────────────────────┐
│ 左侧：Profile 表格/列表                                     │
│ 右侧：当前选中 Profile 详情抽屉                             │
└─────────────────────────────────────────────────────────────┘
```

#### 表格字段

- 选择框
- 名称
- 备注
- 代理状态
- 插件数
- 当前会话
- 最近启动时间
- 状态
- 操作

#### 详情抽屉

Tab 建议：
- 概览
- 浏览器
- 插件
- 任务
- 日志

#### 主要操作

- 新建 Profile
- 编辑 Profile
- 启动 / 停止
- 复制配置
- 删除

#### 空状态

- 没有 Profile 时显示“新建第一个 Profile”
- 支持导入示例配置

### 4.2 浏览器会话页

#### 页面目的
监控所有正在运行的 Chromium 窗口。

#### 布局

```
Header: 浏览器会话 / 搜索 / 状态筛选 / 全部停止
┌─────────────────────────────────────────────────────────────┐
│ 左侧：会话列表                                              │
│ 右侧：会话详情抽屉                                          │
└─────────────────────────────────────────────────────────────┘
```

#### 会话列表字段

- Profile 名称
- 站点
- 窗口状态
- 运行时长
- 代理
- 插件
- 操作

#### 会话详情

- 当前 URL
- 启动时间
- 运行日志
- 内存/CPU 状态占位
- 控制按钮：聚焦、刷新、停止

### 4.3 任务中心页

#### 页面目的
管理站点任务、运行队列和失败记录。

#### 布局

```
Header: 任务中心 / 状态切换 / 新建任务 / 批量运行
┌─────────────────────────────────────────────────────────────┐
│ 左侧：任务表格                                              │
│ 右侧：任务详情 + 运行历史 + 日志                            │
└─────────────────────────────────────────────────────────────┘
```

#### 任务表字段

- 任务名
- 绑定 Profile
- 目标站点
- 状态
- 进度
- 最近运行
- 操作

#### 详情区域

- 任务参数
- 站点 Adapter
- 运行历史
- 错误原因
- 重试按钮

### 4.4 数据结果页

#### 页面目的
查看、筛选、导出采集结果。

#### 布局

```
Header: 数据结果 / 搜索 / 站点筛选 / 导出 JSON / 导出 CSV
┌─────────────────────────────────────────────────────────────┐
│ 上：筛选条                                                  │
│ 中：结果表格                                                │
│ 右：结果 JSON 详情抽屉                                      │
└─────────────────────────────────────────────────────────────┘
```

#### 结果表字段

- 来源站点
- 来源任务
- 来源 Profile
- 标题字段
- 时间
- 状态

#### 详情抽屉

- 原始 JSON
- 结构化字段
- 复制 JSON
- 打开关联任务

### 4.5 设置页

#### 页面目的
管理运行参数、路径、日志、默认行为。

#### 分组

- 运行设置
- 浏览器设置
- 路径设置
- 插件设置
- 日志与诊断

#### 常见表单项

- 最大并发窗口数
- 默认浏览器模式
- Profile 存储路径
- 插件存储路径
- 结果导出路径
- 日志级别

## 5. 交互状态

### 5.1 Loading

- 表格骨架屏
- 抽屉内容骨架屏
- 按钮置灰

### 5.2 Empty

- 明确说明当前无数据
- 给出下一步主动作按钮

### 5.3 Error

- 显示错误摘要
- 提供重试
- 提供查看日志入口

### 5.4 Confirm

- 删除、停止、批量操作必须二次确认
- 危险弹窗使用红色强调

## 6. 推荐信息架构

### Sidebar

1. Profile 管理
2. 浏览器会话
3. 任务中心
4. 数据结果
5. 设置

### 页面优先级

- 默认进入 Profile 管理
- 浏览器会话页用于监控
- 任务中心页用于执行
- 数据结果页用于查看
- 设置页用于配置

## 7. 确认点

- 是否接受“左导航 + 顶部工具栏 + 右侧抽屉”的桌面结构
- 是否接受浅色默认主题
- 是否接受 Profile 详情抽屉里集成插件管理
- 是否接受页面以表格为主，而不是卡片流

